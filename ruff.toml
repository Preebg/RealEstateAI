exclude = ["OGAIUnderwriter.py"]

# Keep the pre-0.16 default rule set. Ruff 0.16+ enables hundreds of extra
# rules by default; without an explicit select, unpinned CI installs break.
[lint]
select = ["E4", "E7", "E9", "F"]
